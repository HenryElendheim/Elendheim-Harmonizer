package com.elendheim.harmonizer.ui.theme

import androidx.compose.ui.graphics.Color

// Dark-first palette, drawn from the logo: three stacked cyans on near-black.
val Background = Color(0xFF0A0C0F)
val Surface = Color(0xFF13181D)
val OnSurface = Color(0xFFE6F3F7)
val Muted = Color(0xFF7E8C93)

val VoiceAccent = Color(0xFF2FD9F2)   // your note (logo bright cyan)
val FifthAccent = Color(0xFF86E5F2)   // the harmony on top (logo light cyan)
val Danger = Color(0xFFE0556B)

// The logo mark's three shades, used for the icon, splash, and header wordmark.
val LogoLight = Color(0xFFCFF1F7)
val LogoBright = Color(0xFF2FD9F2)
val LogoTeal = Color(0xFF2E7C8D)

// The splash stays on near-black with the cyan mark, matching the logo art.
val SplashBackground = Color(0xFF0A0C0F)
